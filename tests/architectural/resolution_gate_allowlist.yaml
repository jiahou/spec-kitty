# Resolution-authority gate allowlist (WP01 / single-authority-resolution-gates-01KW1P0F).
#
# Composite-keyed (enclosing_qualname, line) pre-sweep baseline. Every entry
# carries a mandatory rationale. SHRINK-ONLY: sweep WPs (WP02-WP07) remove
# entries as they route sites; no WP may ADD an entry beyond the baseline.
# Three entries are PERMANENT fixtures, never removed:
#   * canonicalizer _canonicalize_bare_modern_handle (C-001/FR-011 bare probe)
#   * canonicalizer read_primary_meta (seam-internal raw-param first-probe)
#   * coord_authority decisions/emit.py + widen/state.py (by-design coord writes)
#
# WP03 shrink: removed close_cmd:592 (routed through _canonicalize_primary_read_handle).
# WP03 additions: WP03 SANCTION entries for read_primary_meta:820,
#   resolve_handle_to_read_path:950/:972/:1023, _stored_topology_best_effort:1208.
# WP05 shrink (2026-06-26): removed 12 routed canonicalizer entries for the
#   status/runtime/agent sweep (T023-T026):
#     _mid8_from_primary_meta:386, _resolve_coordination_branch:703, _resolve_topology:740,
#     _resolve_mission_id:811, _primary_runtime_feature_dir:98,
#     _mission_routes_through_coordination:177, _primary_anchor_feature_dir:889,
#     _safe_load_meta:138, finalize_tasks:1497, _analysis_report_gate_dir:1094,
#     MissionStatus._find_meta_path:506, MissionStatus.save:747.
# WP05 sanction: MissionStatus._find_meta_path (bare_dir_name) updated line 526->533
#   (shifted by 1 from import addition in _find_meta_path's local import block).
# WP05 coord_authority line updates (workflow.py lines shifted by +7 due to import
#   addition and routing comment in _analysis_report_gate_dir):
#   implement:1268->1275, 1380->1387, 1575->1582; review:2469->2476, 2603->2610,
#   2640->2647, 2725->2732.
# WP07 shrink (2026-06-27): T031 teaches is_def_use_canonical the bare-modern fold
#   seam (_canonicalize_bare_modern_handle). The 4 formerly-sanctioned entries that
#   flow through this seam are now auto-classified canonical by the discriminator,
#   removed from the allowlist (T032):
#     resolve_handle_to_read_path:950/:972/:1023, _stored_topology_best_effort:1208.
#   canonicalizer_baseline tightened 34->3 (3 raw-param permanents remain).
#   coord_authority: all stale entries re-pinned or removed (WP06 routing removed
#     list_dependents:3415 + review:2732 + one list_tasks site from the write census).
#   coord_authority_baseline tightened 18->14 (14 live write-classified sites remain).
# REBASE reconciliation (2026-06-27, design/coord-authority-remediation-2160 onto
#   upstream/main): concurrent mission #1057 inserted check_pre30_layout boundary
#   guards into tasks.py, shifting line numbers AND re-introducing a kind-blind
#   resolve probe in list_dependents (which our WP06 had routed away). Re-keyed the
#   4 drifted tasks.py entries to merged-tree-actual lines (move_task 1082->1081 &
#   1319->1327, list_tasks 2115->2129, validate_workflow 2857->2910) and ADDED
#   list_dependents:3483 as a genuine new #1057 by-design boundary-guard probe.
#   coord_authority_baseline 14->15 and COORD_AUTHORITY_WRITE_FLOOR 14->15 raised to
#   the honest merged-tree live write census (15). Not a sweep-WP inflation: the new
#   entry is a concurrent-mission boundary guard, not our routing regressing.
# coord-read-residuals WP01 (2026-06-27, FR-004/FR-005 identity routing):
#   The #2186 identity routing in workflow.py converted TWO coord-aware
#   resolve_feature_dir_for_mission calls (the sparse-checkout-preflight mission_id
#   read in `implement` + the review-prompt-metadata mission_id read in `review`)
#   to the PRIMARY anchor primary_feature_dir_for_mission(_canonicalize_primary_read_handle(...)).
#   Those two sites are now PRIMARY reads, no longer coord-aware write-census sites:
#     - removed implement:1283 (the preflight resolve_feature_dir_for_mission)
#     - removed review:2739 (the review-prompt resolve_feature_dir_for_mission)
#   The remaining four workflow.py coord-authority entries are pure LINE-DRIFT re-pins
#   (the identity routing added lines above them, no semantic change):
#     implement 1395->1403, 1590->1598; review 2617->2633, 2654->2670.
#   coord_authority_baseline 15->13 and COORD_AUTHORITY_WRITE_FLOOR 15->13 tightened
#   to the honest live write census (13) — a genuine shrink from routing, not a re-pin.

canonicalizer_baseline: 3
coord_authority_baseline: 13

canonicalizer:
  - qualname: _canonicalize_bare_modern_handle
    line: 453
    rationale: "C-001/FR-011 sanctioned bare probe inside _canonicalize_bare_modern_handle (the FR-004 bare-human-slug fold) - folding canonicalization into the primitive would recurse; PERMANENT fixture, never remove"
  - qualname: read_primary_meta
    line: 819
    rationale: "WP03 SANCTION: seam-internal first-probe in read_primary_meta - callers (resolve_handle_to_read_path:929, _stored_topology_best_effort:1203) pass already-canonicalized handles; the raw first-probe is intentional (try literal dir, then _canonicalize_handle fallback in body); wrapping here would double-fold the composed handle; PERMANENT fixture"
  - qualname: MissionStatus._find_meta_path
    line: 533
    rationale: "WP05 SANCTION: bare_dir_name is returned by resolve_bare_modern_mission_dir_name (called on line 531) which returns meta_path.parent.name -- the actual on-disk composed dir name (e.g. 'demo-feature-01ABCDEF'). This is a feature_dir.name-equivalent; the gate cannot detect it via def-use (bare_dir_name is a local str variable, not a .name attribute read) but the invariant is guaranteed by the callee's implementation and docstring. Already-canonical by provenance."

coord_authority:
  # WP02 (T008/T014): ``mark_status:1807`` is ROUTED -- its write leg now uses the
  # kind-aware resolve_planning_read_dir(kind=TASKS_INDEX) (closes #2154), so it is
  # no longer a kind-blind resolve_feature_dir_for_mission write and its entry is
  # removed (the NFR-003 staleness twin-guard would otherwise red-fail on it).
  # The remaining tasks.py / implement.py coord-authority entries below are
  # line-drift RE-PINS only (same kind-blind write site, new line after WP edits
  # shifted the file) -- NOT routed, still pre-sweep baseline for WP02-WP07.
  - qualname: move_task
    line: 1138
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/tasks.py) - #1057 boundary-guard probe (_mt_feature_dir feeds check_pre30_layout); PR #2277 re-pin 1081->1138 (7-WP coexistence line drift)"
  - qualname: move_task
    line: 1396
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/tasks.py) - STATUS-partition coord-aware event-store read; PR #2277 re-pin 1327->1396 (7-WP coexistence line drift)"
  - qualname: list_tasks
    line: 2198
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/tasks.py) - STATUS-partition coord-aware event-log read; PR #2277 re-pin 2129->2198 (7-WP coexistence line drift)"
  - qualname: validate_workflow
    line: 2995
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/tasks.py) - STATUS-partition coord-aware event-log read; PR #2277 re-pin 2926->2995 (7-WP coexistence line drift)"
  - qualname: list_dependents
    line: 3568
    rationale: "#1057-INTRODUCED boundary-guard probe (src/specify_cli/cli/commands/agent/tasks.py): the kind-blind resolve at 3568 feeds check_pre30_layout(feature_dir) (the #1057 pre-3.0 layout hard-reject), then the real dependents READ re-routes through resolve_planning_read_dir(kind=WORK_PACKAGE_TASK). Same by-design pattern as move_task:1138. The function is WRITE-classified by the conservative function-granularity predicate (json.dumps in the body), but the kind-blind probe is not the write target. Added during the upstream/main rebase: our WP06 had routed list_dependents away (removed list_dependents:3415), but concurrent mission #1057 independently inserted a check_pre30_layout boundary guard here that requires a raw kind-blind resolve — a genuine new by-design coord-owned probe. PR #2277 re-pin 3499->3568 (7-WP coexistence line drift)."
  - qualname: implement
    line: 1403
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/workflow.py) - coord-read-residuals WP01 line-drift re-pin 1395->1403 (identity routing added lines above)"
  - qualname: implement
    line: 1598
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/workflow.py) - coord-read-residuals WP01 line-drift re-pin 1590->1598 (identity routing added lines above)"
  - qualname: review
    line: 2633
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/workflow.py) - coord-read-residuals WP01 line-drift re-pin 2617->2633 (identity routing added lines above)"
  - qualname: review
    line: 2670
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/agent/workflow.py) - coord-read-residuals WP01 line-drift re-pin 2654->2670 (identity routing added lines above)"
  - qualname: implement
    line: 1125
    rationale: "pre-sweep baseline write (src/specify_cli/cli/commands/implement.py detect-feature-context block) - same pre-existing resolve_feature_dir_for_mission(repo_root, mission_slug) call (git blame: last edited 2026-06-04, predates this mission); lifecycle-tooling-friction WP02/WP05 inserted ~99 lines above it, line-drift re-pin 1022->1121 plus null-diff sentinel patch 1121->1125 (NOT a new kind-blind write); will be routed in a future sweep WP"
  - qualname: _mission_dir
    line: 71
    rationale: "legitimate coord-owned write that bypasses commit_for_mission by design - coord status authority is at the caller level, not the commit level; PERMANENT fixture"
  - qualname: WidenPendingStore.__init__
    line: 63
    rationale: "legitimate coord-owned write that bypasses commit_for_mission by design - coord status authority is at the caller level, not the commit level; PERMANENT fixture"
