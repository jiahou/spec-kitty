# Issue matrix — mission-identity-seam-and-1908-panel-01KV6510

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #1908 | Mission identity panel (#1929 cluster B) | deferred-with-followup | Panel umbrella (the PR whose adversarial review surfaced cluster B). Its findings are addressed by #1915 (WP03), #1917 (WP07), #1916 (WP08); #1908 itself is not a single closeable bug — ADVANCED, residual panel items tracked under #1929. |
| #1978 | mid8 naming seam — P1 merge-blocker driver | fixed | lane-b WP02, commit f7627f773 (3 merge false-compose sites routed via mission_branch_name_required; RED-first tests/merge/test_mid8_embedded_preflight.py reproduces the NNN-+mid8 false-negative; fail-closed preserved, legacy resolves) |
| #1949 | Legacy-slug idempotency in branch naming | fixed | lane-a WP01 (seam core): `mission_branch_name`/`lane_branch_name` made idempotent on the `mission_id=None` path (the genuine residual — a slug carrying both an `NNN-` prefix AND an embedded mid8 no longer double-/mis-composes); regression-LOCK + genuinely-RED `mission_id=None` test in tests/lanes/test_branch_naming_seam.py. |
| #1918 | mid8_from_slug dual-era false-positive (coincidental 8-char tail) | fixed | lane-a WP01 demoted `mid8_from_slug` in place to a non-authoritative heuristic (declines on a coincidental tail with no mission_id) + added authoritative `resolve_mid8(slug, *, mission_id)`; lane-j WP10 (commit 38f0bdc47) routed the value-callers (decision.py, agent/{mission,workflow,context}.py) to `resolve_mid8`, keeping the safe boolean-detector uses; #1918 regressions prove coincidental tails no longer mis-resolve. |
| #1899 | Allocator worktree name routed through canonical seam (no ad-hoc f-string) | fixed | lane-c WP03, commit c37fd9ad0 (allocator routes worktree_path(mission_id=None); byte-identical vs GOLDEN_ROWS legacy row). Enforcement closeout: lane-i WP09, commits aa48e471a+fda8f9ca6 (literal-ban ratchet — the worktree-grammar twin / 4th assertion — now lands: tests/architectural/test_no_worktree_name_guess.py forbids worktree/branch/mid8 name-guessing outside lanes/branch_naming.py; bites all 3 recurrence idioms (probe RED then reverted; detector confirmed to flag, not miss, the allow-listed sites at exact file:line); allow-list audited benign/justified; reviewer-renata approved). |
| #1868 | mid8 seam epic (Canonical seams — bind authority to type/owner) | deferred-with-followup | Epic, not a closeable bug. ADVANCED: this mission established the single canonical `lanes/branch_naming.py` authority and routed all compose/parse/worktree/coord sites through it (#1978/#1949/#1918/#1899 fixed under it). Broader epic remains open for future seams. |
| #1915 | Multi-dependency lane merge non-atomic (earlier clean dep merge survives later conflict) | fixed | lane-c WP03, commit c37fd9ad0 (snapshot pre-loop HEAD + reset --hard on any later-dep conflict; RED-first regression in tests/lanes/test_worktree_allocator_atomicity.py) |
| #1917 | base-ref `--` end-of-options separator in `_validate_base_ref` | fixed | lane-g WP07, commit c1ce1e144 (`_validate_base_ref` passes the `--base` value after a `--` separator to `git rev-parse --verify`; RED-first arg-injection regression in tests/specify_cli/cli/commands/test_implement_base_ref.py; reviewer-renata approved). |
| #1916 | Panel cluster B (accept readiness side-effect-free) | fixed | lane-h WP08, commits f1faafb18 + 4fad32d23 (all 3 readiness write-paths closed under default SaaS-enabled config: get_emitter read-only resolve_identity + seeded _identity/_git_resolver, resolve_identity read-only, is_sync_enabled_for_checkout via read-only twin; stopgap _filter_accept_owned_project_config retired; no global persist flag; test_get_emitter_does_not_persist_identity green under autouse SPEC_KITTY_ENABLE_SAAS_SYNC=1) |
| #1929 | Rest-of-panel meta-checklist (cluster B) | deferred-with-followup | META-checklist tracker, intentionally NOT closed/re-parented (per spec scope). Its cluster B items addressed this mission: #1915 (WP03), #1917 (WP07), #1916 (WP08) all fixed; remaining checklist items stay tracked under #1929. |
| #1860 | branch-identity dual-era wrong-compose on status-read path | verified-already-fixed | Closed before this mission (the prior instance of the recurring wrong-compose class). This mission hardens against its RECURRENCE: the single canonical seam (WP01) + the literal-ban ratchet (WP09, which bites the exact `endswith(f"-{mid8}")`/`f"{slug}-{mid8}"` shape #1860 was) make the class structurally non-reintroducible. Regression test tests/regression/test_issue_1860_branch_identity_dual_era.py remains green. |
| #1878 | mid8 seam-related | deferred-with-followup | WP06 (lane-f, commit 7fbe47c65): FR-010 satisfied — coordination/missions composers unified onto the seam (4 coord read/transaction composers + _create.coordination_branch_name + _read_path_resolver._compose_mission_dir delegate to lanes.branch_naming coord_*/canonical primitives; no second algorithm survives). #1878 broader strangler ADVANCED, not closed. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
