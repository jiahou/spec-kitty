---
work_package_id: WP13
title: Lockfile regen → drift 0 (generated == committed) from the backfilled frontmatter
dependencies:
- WP12
- WP08
requirement_refs:
- FR-010
- NFR-006
tracker_refs: []
planning_base_branch: docs/2165-mission-b-structural-move
merge_target_branch: docs/2165-mission-b-structural-move
branch_strategy: Planning artifacts for this mission were generated on docs/2165-mission-b-structural-move. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into docs/2165-mission-b-structural-move unless the human explicitly redirects the landing branch.
subtasks:
- T076
- T077
- T078
- T079
agent: "claude:opus:python-pedro:implementer"
history: []
agent_profile: python-pedro
authoritative_surface: docs/development/3-2-page-inventory.yaml
create_intent: []
execution_mode: code_change
owned_files:
- docs/development/3-2-page-inventory.yaml
role: implementer
tags: []
shell_pid: "1933182"
---

## ⚡ Do This First: Load Agent Profile

Before reading anything else, load your agent profile: run `/ad-hoc-profile-load python-pedro` (or read `src/doctrine/agent_profiles/built-in/python-pedro.agent.yaml` and adopt it). State which directives apply, then proceed.

## Objective

Regenerate the page-inventory lockfile **FROM the backfilled frontmatter** (the SSOT inversion: frontmatter is the source, the lockfile the derived rollup) so the live `--strict` drift (**252 removed / 296 changed / 0 added**) closes to **0** (generated == committed). This is IC-05f / NFR-006 — the precondition for WP14's lockfile-gate flip (flipping before drift = 0 self-blocks the merge).

## Context

`occurrence_map.yaml` `inventory_stays_put` + data-model `Page-inventory lockfile` entity are the authority. The lockfile **STAYS at `docs/development/3-2-page-inventory.yaml`** (operator directive); the 4 lockfile constants are UNCHANGED. It is regenerated by `scripts/docs/inventory_lockfile.py` (Mission A's generator — **consumed, not rewritten** here) via `run_generate_and_compare(docs_root, inventory, repo_root, strict)`.

**Ordering (FR-010):** drift closes to 0 **only AFTER** the move (WP03) lands all content under `docs/` **and** the backfill (WP12) authors the frontmatter. So WP13 depends on WP12 (frontmatter) **and** WP08 (the references are coherent — a stale reference could leave the tree inconsistent). Wrong ordering reports false drift.

**Rollup semantics that must survive:** completeness ("every `.md` inventoried"), workstream ownership, deterministic alphabetical diff. The lockfile is **derived** — frontmatter is SSOT.

## Requirement refs (hints for the orchestrator's map-requirements)

FR-010 (regenerate the lockfile, pass the generate-and-compare freshness gate), NFR-006 (lockfile regenerates deterministically; generated == committed; drift closed from 252-removed/296-changed to 0).

## Subtasks

### T076 — Regenerate the lockfile from frontmatter
Run `scripts/docs/inventory_lockfile.py` (`run_generate_and_compare`) over the moved + backfilled `docs/` tree, writing the regenerated `docs/development/3-2-page-inventory.yaml`. The generator is consumed as-is (not modified). Confirm the file stayed at its path (operator directive).

### T077 — Drive drift to 0
Confirm the regenerated lockfile closes the live `--strict` drift (252 removed / 296 changed / 0 added) to **0**. A residual removed/changed entry means a page's frontmatter is missing or wrong (back to WP12) or a reference is stale (back to WP08) — surface it, do not hand-edit the lockfile to mask drift.

### T078 — Prove generate == committed (deterministic)
Run the generator twice and confirm identical output (deterministic, alphabetical diff). Confirm the committed lockfile equals a fresh regen — this is the NFR-006 generate-and-compare invariant the CI freshness gate keys on. The rollup semantics (completeness, workstream ownership) survive.

### T079 — Verify + hand off to WP14
Confirm drift = 0 with `inventory_lockfile.py --strict` AND the legacy `check_docs_freshness.py` (still report-only here — WP14 flips it). This drift-0 state is the precondition for WP14's lockfile-gate code flip. Terminology guard clean.

## Surfaces & Loci

| Surface | Role | Notes |
|---------|------|-------|
| `docs/development/3-2-page-inventory.yaml` | regenerated (STAYS PUT) | the only owned file; derived from frontmatter |
| `scripts/docs/inventory_lockfile.py` | consumed (NOT modified) | `run_generate_and_compare(docs_root, inventory, repo_root, strict)` → `InventoryDrift(added, removed, changed)` |
| `scripts/docs/check_docs_freshness.py` | read-only here | report-only until WP14 flips it |

**Live drift to close:** 252 removed / 296 changed / 0 added → **0**. Rollup semantics that must survive: completeness, workstream ownership, deterministic alphabetical diff.

## Requirement → Subtask Traceability

| Requirement | Subtasks |
|-------------|----------|
| FR-010 (regenerate lockfile, pass generate-and-compare) | T076, T077, T079 |
| NFR-006 (deterministic; generated == committed; drift→0) | T077, T078, T079 |

## Branch Strategy

Planning + final merge target: `docs/2165-mission-b-structural-move`. Depends on WP12 (frontmatter SSOT) + WP08 (coherent references). Precondition for WP14 (the gate flip needs drift = 0 first).

## Definition of Done

- [ ] Lockfile regenerated **FROM frontmatter** via `inventory_lockfile.py` (generator consumed, not modified); stays at `docs/development/3-2-page-inventory.yaml`.
- [ ] Live `--strict` drift (252 removed / 296 changed / 0 added) closed to **0** — no lockfile hand-edit to mask drift.
- [ ] **Deterministic**: two regens identical; committed == fresh regen (NFR-006 generate-and-compare).
- [ ] Rollup semantics survive (completeness, workstream ownership, alphabetical diff).
- [ ] **No reference/runtime break introduced**: the lockfile is a derived artifact; the SSOT is the frontmatter (WP12).
- [ ] drift = 0 confirmed under both `inventory_lockfile.py --strict` and `check_docs_freshness.py`; precondition for WP14 met.

## Risks & Reviewer Guidance

- **Reviewer (NFR-006 focus):** confirm drift = 0 is achieved by **correct frontmatter**, not by hand-editing the lockfile (which would defeat the generate-and-compare invariant).
- **Wrong ordering** (regenerating before WP12's backfill or WP08's references land) reports false drift — confirm both deps are in.
- A residual drift entry is a real signal (a missing/wrong page) — route it back to WP12/WP08, do not mask it.

## Activity Log

- (populated at implement time)
- 2026-06-27T17:46:30Z – claude:opus:python-pedro:implementer – shell_pid=1933182 – Assigned agent via action command
- 2026-06-27T17:54:13Z – user – shell_pid=1933182 – Moved to planned
- 2026-06-27T19:28:41Z – user – shell_pid=1933182 – done+validated on assembled integration tree (571 docs tests green, 5 gates green); lane alloc impossible (diamond merge)
- 2026-06-27T19:28:43Z – user – shell_pid=1933182 – approved: assembled-tree validation is the objective review (571 docs tests + 5 blocking gates green; WP14 C-005 RED-per-class proven)
