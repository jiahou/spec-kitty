# Resume Review — Researcher Robbie
**Mission**: doctrine-glossary-architecture-consolidation-01KTNWFC
**Reviewer**: researcher-robbie
**Date**: 2026-06-11
**Scope**: Verify research decisions R-01…R-10, C-005, occurrence_map, and analysis-report against the current tree after upstream missions 01KTRC04 (guard consolidation / tooling stability) and 01KTPKST (execution-context unification).

---

## 1. Research Decisions (research.md R-01…R-10)

| Item | Claim / Factual Basis | Verdict | Notes |
|------|-----------------------|---------|-------|
| **R-01** — glossary promoted to top-level canonical surface | "Glossary is PROMOTED to a canonical top-level `glossary/` surface (not under architecture)" + "all path + code references are updated" | **STALE (partial)** | `glossary/` and `glossary/contexts/` already exist as the canonical surface — they were established 2026-03-10 (commit `299822c1`). `architecture/glossary/` has only a pointer `README.md` (no substantive content to move). Charter `governance.yaml` already references `glossary/contexts/` (top-level). The "promotion" is structurally done; what remains is: cleanup of residual `architecture/glossary/README.md`, content refresh (FR-010), scope-defer record (FR-011), and updating docs prose that still mentions `architecture/glossary` (found in `docs/development/3-2-version-taxonomy.md` and `docs/development/3-2-page-inventory.yaml`). WP01 T002 "hard move" task is therefore narrower than implied. |
| **R-02** — versioned dirs + living-architecture-at-top | "Top-level `architecture/` = current + future architecture; `architecture/<version>/` = traceability record" | **CURRENT** | `architecture/1.x/`, `2.x/`, `3.x/` all exist. `architecture/3.x/adr/` is receiving new ADRs. Top-level `architecture/` has no `vision/` or `diagrams/` dirs yet — these are still planned. The decay model matches the tree. |
| **R-03** — vision/ home in architecture/ | "Top-level `architecture/vision/` holds current+future vision" | **CURRENT** | No `vision/` dir exists at top-level `architecture/` yet — that is the work. No conflicting `docs/vision/` was created meanwhile. Claim correct as a forward decision. |
| **R-04** — C4 hand-authored Markdown + Mermaid | "Keep existing convention — hand-authored Markdown + Mermaid; FR-006 refresh = carry level-structured Markdown forward; 2.x keeps snapshot" | **CURRENT** | Confirmed: `architecture/2.x/{01_context,02_containers,03_components}/` contain `.md` files. No `diagrams/` dir exists at top level yet; the carry-forward is still pending. No tooling change (generated-C4 swap deferred to #1812 — still open). |
| **R-05** — hard moves + reference updates, no stubs | "Hard moves; bulk-edit applied selectively via ICs; each WP gets occurrence_map of old→new paths" | **STALE (wording)** | The mission-level `change_mode: bulk_edit` was reverted (analysis-report O1 remediation, commit `dc67e90d7`). The `meta.json` carries no `change_mode`. However `tasks.md` header still reads `change_mode: bulk_edit` — a stale artifact header not yet reconciled with the revert. The occurrence_map was repurposed as a checklist (correct). WP01/WP02 task bodies still reference "bulk_edit discipline" which is intentional (per R-05 "via their ICs"), but individual WP frontmatter has no `change_mode` field, so the gate will not trigger. This is consistent with the revert but the tasks.md header is misleading. |
| **R-06** — phased dependency: moves → authoring; code lanes parallel | "Phased: (1) freeze layout → (2) moves/rewrites → (3) author new content → (4) code WPs parallel from start" | **CURRENT** | WP ordering (WP01→WP02 Phase 1; WP03–WP06 Phase 2; WP08/WP09 parallel code lanes) still matches this model. No new dependency or blocker landed between missions. |
| **R-07** — lane ownership by profile (guidance, not assignment) | "Capture profile diversity need; let finalize-tasks/runtime routing decide; add review ICs naming recommended reviewer" | **CURRENT** | WP task files carry `agent_profile` frontmatter and review ICs. No profile was removed or renamed that would invalidate the recommendations. |
| **R-08** — glossary runtime-scope defer | "Defer runtime-scope promotion; glossary content updates happen NOW" | **CURRENT** | `GlossaryScope` enum unchanged (`mission_local/team_domain/audience_domain/spec_kitty_core`). The `planning-and-tracking.yaml` seed file was added to `.kittify/glossaries/` (commit `040cc7ce8`) as a non-registered subset — exactly the intended state. FR-011 defer path is intact. |
| **R-09** — DRG: close gaps in existing generator (regen command + symmetric edge detection) + re-curate | "Close gaps in the existing generator — NOT a rebuild. PLUS sanitize/re-curate the built-in DRG" | **CURRENT (with new context)** | The claim that "today there's no regeneration command" remains true: no `spec-kitty drg regenerate` or equivalent CLI command exists as of the current tree. `graph.yaml` is generated via `doctrine.drg.migration.extractor.generate_graph()` (internal tool only). **NEW FACT (material):** `DRGNode` and `DRGEdge` now carry a declared `provenance: str | None` field (landed in 01KTRC04, commit `83542044c`). `_tag_source()` uses `model_copy(update={"provenance": source})`. Crucially, `_write_graph_yaml()` uses an explicit field-by-field serializer (`_node_to_dict` writes only `urn/kind/label`; `provenance` is excluded), so **graph.yaml output stays stable**. WP09 can proceed without accounting for provenance serialization — but the WP09 owned_files list (`src/doctrine/drg/**`, `src/glossary/drg_builder.py`) is still correct. |
| **R-10** — charter `extends:` additive merge + cycle detection | "Additive merge, base-org precedence + cycle detection, integrated with existing charter.activation_engine/cascade. No parallel mechanism." | **CURRENT** | `charter.activation_engine` and `charter.cascade` are unchanged in shape by 01KTRC04/01KTPKST. WP08 owned files (`src/charter/`) are correct. No new parallel charter mechanism was introduced. |
| **C-005** — consolidate; eliminate parallel mechanisms | "Through-line: no new parallel mechanisms; each surface has a single source of truth after the mission" | **CURRENT (reinforced)** | 01KTRC04 explicitly consolidated the commit-guard into `src/specify_cli/core/commit_guard.py` (single policy seam — `C-GUARD-1`). This is thematically aligned with C-005 and demonstrates the pattern working. The surface resolver (`coordination/surface_resolver.py`) was also consolidated (config-determined placement). Neither change conflicts with this mission's scope. |
| **R-03 / FR-012** — #391 usage-test | "#391 as doctrine usage-test; split, reparent, close as superseded" | **CURRENT** | CLAUDE.md memory confirms #391 debt/sanitization is tracked at #391. No evidence that #391 was reorganized or closed between mission creation and now. The dogfood task (WP11) is still valid. |

---

## 2. Occurrence_map (reference-rewrite checklist)

| Category / Claim | Checklist Accuracy | Verdict | Notes |
|------------------|--------------------|---------|-------|
| **Moves: glossary_promotion FROM** `architecture/glossary/` | Listed as a FROM source for path rewrites | **STALE** | `architecture/glossary/` has only a pointer README; no content to move. The rewrite is a cleanup (remove old pointer doc, keep contexts at top-level `glossary/`). The rewrite surface is narrower than the move implies. |
| **Moves: glossary_promotion FROM** `.kittify/glossaries/ (canonical source)` | Listed as a move source | **STALE (label error)** | `.kittify/glossaries/` holds machine-readable YAML seeds for the glossary pipeline. `scope.py:load_seed_file()` already loads from `repo_root / ".kittify" / "glossaries"`. These files are NOT moving and do not need rewriting. The label "canonical source" in the occurrence_map is misleading — these are pipeline inputs, not the human glossary docs. The actual source of truth for glossary contexts is `glossary/contexts/` (already exists). |
| **Moves: glossary_promotion FROM** `src/glossary content paths` | Listed in move sources | **CURRENT** | `src/glossary/scope.py` contains the `GlossaryScope` loader and already points to `.kittify/glossaries/` for seeds. No path in `src/glossary` references `architecture/glossary`. The T003 task ("point src/glossary/scope.py at the canonical glossary/ location") needs to confirm no hardcoded `architecture/glossary` references exist — confirmed none found in current tree. T003 scope may be minimal. |
| **filesystem_paths: rewrite** — charter authority paths | Charter `governance.yaml` and `charter.md` reference old `architecture/2.x/adr/` and `architecture/adrs/` | **CURRENT (rewrite still needed)** | `governance.yaml` authority_paths still list `architecture/2.x/adr/` and `architecture/adrs/`. These are active references that need updating as part of WP02 (architecture restructure). The `glossary/contexts/` reference is already correct. |
| **filesystem_paths: rewrite** — doctrine artefacts + SKILL.md files | Referenced in notes as doc cross-links | **STALE (partially)** | `src/doctrine/base.py` and `src/doctrine/README.md` still reference `architecture/2.x/adr/2026-...`. `src/doctrine/skills/spec-kitty-mission-review/SKILL.md` references `architecture/2.x/adr/2026-04-26-3-e2e-hard-gate.md`. Several tactic YAML files in `src/doctrine/tactics/` reference `architecture/adrs/`. These are real rewrite targets still valid. **However**: `spec-kitty-mission-system/SKILL.md` was modified by commit `16a831b23` (2026-06-11) to remove phantom profile-hierarchy commands — the SKILL.md reference surface has changed since the occurrence_map was drafted. The rewrite target count for SKILL.md files may differ from what the implementer expects. |
| **tests_fixtures: rewrite** — glossary loader tests | "Tests asserting glossary/architecture paths" | **CURRENT** | Found `tests/architectural/test_no_legacy_terminology.py` references `.kittify/glossaries/spec_kitty_core.yaml` (correct current location). Tests in `tests/agent/glossary/` create seed files in `.kittify/glossaries/` (correct). No test asserts `architecture/glossary` path. The rewrite burden in tests is lighter than the checklist implies — primarily around architectural authority-path assertions. |
| **doctor.py god-module split** — `_profile_health_render.py` extracted | Occurrence_map drafted before doctor.py split | **STALE (new file)** | `src/specify_cli/cli/commands/_profile_health_render.py` (337 lines) was extracted from `doctor.py` as part of 01KTRC04 (commit `83542044c`). `doctor.py` itself is still large (3,093 lines — #1623 full split is deferred). WP implementations that reference `doctor.py` for profile-health context must also account for `_profile_health_render.py`. Not a blocker, but the occurrence_map's `code_symbols` review should note this new file. |
| **charter/mission_steps.py facade** | Not explicitly inventoried in occurrence_map, but relevant to doctrine surface | **STALE (symbol retired)** | `MissionStep` was retired from `charter.mission_steps.__all__` (commit `9fe827f79`, 2026-06-11). The live facade contract is now `[MissionStepContract, MissionStepInput, MissionStepContractRepository, MissionStepContractStep, MissionStepRepository]`. The occurrence_map code_symbols review should confirm no WP references a retired symbol. |

---

## 3. Analysis-report

| Finding | Accuracy | Verdict | Notes |
|---------|----------|---------|-------|
| **O1 resolved** — bulk_edit reverted at mission level | "Both primary + coord `meta.json` now carry no `change_mode`" | **CURRENT** | Confirmed: `meta.json` has no `change_mode`. However `tasks.md` header still says `change_mode: bulk_edit` — minor stale artifact not a blocker. |
| **C2 (MEDIUM)** — WP02 edits charter's own authority paths | "Implementer verifies charter still loads + `doctor` healthy post-move" | **CURRENT** | Charter authority paths in `governance.yaml` still list `architecture/2.x/adr/` and `architecture/adrs/` — the self-referential edit is still needed and the risk remains valid. |
| **U1 (LOW)** — WP09 DRG regeneration entry point | "Assumed at `src/doctrine/drg/**`" | **CURRENT (but updated)** | Entry point is `src/doctrine/drg/migration/extractor.py:generate_graph()`. This is internal; no regeneration CLI command yet — WP09 will need to expose one. Correctly flagged as a gap. |
| **Coverage 100%, no CRITICAL/HIGH** | "Clear to implement" | **CURRENT** | No new critical issues introduced by the two landed missions. |

---

## 4. New Facts Not in the Research (Material to the Mission)

| ID | New Fact | Impact |
|----|----------|--------|
| NF-01 | **`DRGNode`/`DRGEdge` gained declared `provenance: str | None` field** (01KTRC04 / 83542044c). `_tag_source()` now uses `model_copy(update={"provenance": source})`. The extractor's field-by-field writer excludes `provenance` from `graph.yaml`. | WP09 (DRG generator): the regeneration command implementer must ensure `provenance` is NOT written to `graph.yaml` (already guaranteed by `_node_to_dict`). The freshness gate is unaffected. The DRG data model is richer — WP10 re-curation can read `.provenance` to audit built-in vs project nodes. |
| NF-02 | **`mission_runtime` is now the canonical execution-state surface** (01KTPKST, ADR `2026-06-07-1-execution-state-canonical-surface.md`). Exports: `CommitTarget`, `CommitTargetKind`, `resolve_action_context`. | Affects WP06 (Ops ADR): the ADR must reference `mission_runtime` for execution-context resolution, not the legacy coord-split surfaces. The C4 refresh (WP03) containers layer must include `mission_runtime` as a named module boundary. |
| NF-03 | **Guard consolidated into `src/specify_cli/core/commit_guard.py`** (01KTRC04). Single policy seam (`C-GUARD-1`). `safe_commit` is mechanism-only; `commit_guard.evaluate` is the one decision. | Affects WP06 (Ops ADR): any Op lifecycle description touching commits should reference the guard module. C4 component view (WP03) should reflect this separation. Does not block any WP. |
| NF-04 | **Config-determined placement shipped** (commits `760f8ca02`, `2ae7c8d03`): `surface_resolver.py` now anchors meta.json reads on the primary checkout before topology resolution (FR-003 cascade layers 1-4). | Affects WP03 (C4): the coordination topology / surface authority surface has changed shape. The container/component diagrams may need to reflect the config-anchored resolution path if they include status/surface detail. |
| NF-05 | **`agent profile list/show` is the active surface for profiles** (01KTB6AN, commit `b7bf08614`+successors). The phantom `agent profile hierarchy/init/create` commands were removed from `spec-kitty-mission-system/SKILL.md` (commit `16a831b23`). | WP10 (DRG re-curation): profile documentation in any authored doctrine artefact must reference `spec-kitty agent profile list/show` — not any hierarchy command. WP05 (toolguide) and WP04 (procedures) that touch tooling references should use the current surface. |
| NF-06 | **`_profile_health_render.py` extracted from `doctor.py`** (01KTRC04). `doctor.py` is still 3,093 lines (#1623 full split deferred). | WP09/WP10 (DRG code work): when navigating profile-health output for freshness-gate verification, look in both `doctor.py` and `_profile_health_render.py`. |
| NF-07 | **`MissionStep` retired from `charter.mission_steps.__all__`** (commit `9fe827f79`, 2026-06-11). Live facade: `[MissionStepContract, MissionStepInput, MissionStepContractRepository, MissionStepContractStep, MissionStepRepository]`. | WP08 (charter extends): if any WP08 code imports from `charter.mission_steps`, it must not reference the retired `MissionStep` symbol. Architectural test `test_charter_facades_reexport_doctrine.py` enforces the live table. |
| NF-08 | **Target branch retargeted from `fixups/code-engine-stabilization` to `feat/doctrine-glossary-consolidation-01KTNWFC`** (commits `0e8eaf414`, `1c9edb42c`). `meta.json` is correct; WP frontmatter is correct. But `spec.md` header (`**Target branch**: fixups/code-engine-stabilization`) and `tasks.md` header (`**Branch**: fixups/code-engine-stabilization`) were NOT updated. | Minor artifact inconsistency. Implementer should work against `feat/doctrine-glossary-consolidation-01KTNWFC` (per meta.json and WP frontmatter) and treat the two stale prose references as known stale. Not a blocker. |
| NF-09 | **`glossary/` top-level surface was established before this mission** (commit `299822c1`, 2026-04-05; `glossary/contexts/` populated through mid-2026). `architecture/glossary/README.md` already points to the root surface. | WP01 T002 ("hard move glossary content → top-level `glossary/`") is narrower than implied: the content ALREADY lives at `glossary/contexts/`. Remaining work = delete/archive the `architecture/glossary/README.md` pointer, update residual docs-prose references (`docs/development/3-2-version-taxonomy.md`, `docs/development/3-2-page-inventory.yaml`), confirm no broken links. |

---

## 5. Verdict

**NEEDS-REFRESH** on the following items (others are CURRENT):

| Item | Refresh Needed |
|------|----------------|
| R-01 | WP01 scoping note: glossary promotion is partially done; move task (T002) is much narrower. Update the WP01 "Context" block and occurrence_map `glossary_promotion.from` description to reflect current reality before implement. |
| R-05 / tasks.md | tasks.md header still says `change_mode: bulk_edit` (should say none or remove); stale since remediation. Minor but could confuse the bulk-edit gate check. |
| occurrence_map `glossary_promotion.from` | Remove `.kittify/glossaries/ (canonical source)` from the FROM list or annotate it clearly as "seed pipeline — NOT moving". Correct `architecture/glossary/` to note it has only a pointer README. |
| occurrence_map `code_symbols` review | Add `_profile_health_render.py` and the retired `MissionStep` symbol as explicit items to check. |
| occurrence_map `user_facing_strings` | Add `spec-kitty-mission-system/SKILL.md` as changed (phantom commands removed); update expected rewrite targets for SKILL.md files. |
| NF-01 (`provenance`) | Note in WP09 context that `DRGNode`/`DRGEdge` gained `provenance`; confirm extractor excludes it (already does). |
| NF-02 (`mission_runtime`) | Update WP06 context and WP03 C4 container list to reference the canonical `mission_runtime` surface. |
| NF-08 (target branch) | Update `spec.md` header and `tasks.md` header branch field from `fixups/code-engine-stabilization` to `feat/doctrine-glossary-consolidation-01KTNWFC`. |
| NF-09 (glossary already exists) | Confirm in WP01 brief that the top-level `glossary/` surface is already established; T002 is cleanup+validation, not a fresh move. |

**Items confirmed CURRENT:** R-02, R-03, R-04, R-06, R-07, R-08, R-09 (core claim), R-10, C-005, analysis-report findings C2/U1/M1/N1.

**Overall research basis:** Structurally SOUND. The core decisions (architecture boundary rules, versioned history model, DRG approach, charter extends design, glossary scope defer) are all consistent with the current tree. The refresh items are scope clarifications and stale cross-references from interim missions — they do not invalidate any decision or require re-specifying.
