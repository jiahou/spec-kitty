# Tracer: Tooling Friction — sync-strict-json-auth-01KWA6KN

Append every place the tooling fought us. Feeds the tooling-gap backlog.

- 2026-06-29: Governance DIR-012 (assign tracker issue to HiC) cannot be satisfied — `gh issue edit 2254 --add-assignee MOES-Media` returns `MOES-Media does not have the correct permissions to execute ReplaceActorsForAssignable` (fork owner can't be an assignee on upstream `Priivacy-ai/spec-kitty`). Known, documented limitation; noted and continued.
- 2026-06-29: `spec-kitty upgrade` rebuilt the CLI from local repo source (3.2.2 → 3.2.4), not the released PyPI artifact, because this repo is the editable install. Expected for dogfooding.
