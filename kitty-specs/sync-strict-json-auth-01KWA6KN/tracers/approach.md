# Tracer: Approach — sync-strict-json-auth-01KWA6KN

What we tried, what worked, what we'd do differently.

- 2026-06-29: Reproduced the failure on pristine `origin/main` (`ccd278061`) before any change — `test_mission_create_json_strict_when_sync_skips_ingress` fails with `sync.server_auth_failure` at `final_sync` ("Not authenticated: no valid access token"). Live evidence captured (§4).
- Plan: §4 red-first discipline — decide whether the failing test is stale (re-pin), stub/scaffold (delete), or valid (fix product) via FR-001 root-cause determination, NOT by softening the assertion.
- 2026-06-29: Independent code-truth root-cause map (Explore/codegraph) → verdict: test-seeding drift from commit `a75174917` (#2182). Verified file:line.
- 2026-06-29: Post-plan adversarial pass (debugger-debbie + reviewer-renata lens) findings folded into plan: (1) root-cause + schema verdicts SURVIVE; (2) fix sufficiency SURVIVES but has a logging-reachability residual — the `_team` WARNING reaching subprocess stderr is the one link static reading can't fully close → MUST live-verify after re-pin (§4); (3) original IC-02 path-equality lock was VACUOUS (tautological) → replaced with resolver-anchored seeding (drift-class kill) + negative auth pin (assert "no valid access token"/"Not authenticated" absent from stderr).
- 2026-06-29 LIVE EVIDENCE (T004) — the residual was REAL; FR-001 was incomplete. After re-pinning seeding, the auth failure is GONE (negative pin passes — session loads). But the test still fails: the genuine skip is signaled on stderr as `diagnostic_code=sync.server_auth_failure ... Detail: skipped: no Private Teamspace available for direct ingress`. TWO production-side problems exposed: (A) MISCLASSIFICATION — `classify_sync_error` (diagnostics.py:91 catch-all) maps the benign "no Private Teamspace" skip to `sync.server_auth_failure`, dropping the canonical `direct_ingress_missing_private_team` category (operator-facing bug: tells you to `auth login` when auth is fine); (B) the `_team` "sole skip diagnostic" WARNING (specify_cli.sync._team) never reaches the mission-create subprocess stderr. So the real fix needs a small PRODUCTION change, not just test-seeding — covered by FR-005. Proposed minimal fix: add `DIRECT_INGRESS_MISSING_PRIVATE_TEAM = "sync.direct_ingress_missing_private_team"` to SyncDiagnosticCode + a classify_sync_error branch for the skip text (before the auth/catch-all). Blast radius verified LOW — no existing test pins the skip case to server_auth_failure (all such assertions use network down / 401 / token / no-valid-access-token). This satisfies the test's existing assertion (stderr diagnostic_code then contains `direct_ingress_missing_private_team`) AND fixes the real bug.
