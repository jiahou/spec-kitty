---
work_package_id: WP01
title: 'Foundation: Source Doc + Epic Re-parent'
dependencies: []
requirement_refs:
- FR-001
- FR-002
- FR-004
tracker_refs: []
planning_base_branch: design/doctrine-catfooding-2196
merge_target_branch: design/doctrine-catfooding-2196
branch_strategy: Planning artifacts for this mission were generated on design/doctrine-catfooding-2196. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into design/doctrine-catfooding-2196 unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
phase: Phase 0 - Foundation
assignee: ''
agent: "claude:opus:reviewer-renata:reviewer"
shell_pid: "1699534"
history:
- at: '2026-07-01T06:14:46Z'
  actor: system
  action: Prompt generated via /spec-kitty.tasks
agent_profile: implementer-ivan
authoritative_surface: docs/development/
create_intent:
- docs/development/quality-and-tech-debt-standing-orders.md
execution_mode: code_change
model: ''
owned_files:
- docs/development/quality-and-tech-debt-standing-orders.md
- docs/development/3-2-page-inventory.yaml
role: implementer
tags: []
task_type: implement
---

# Work Package Prompt: WP01 – Foundation: Source Doc + Epic Re-parent

## ⚡ Do This First: Load Agent Profile

Use the `/ad-hoc-profile-load` skill to load the agent profile specified in the frontmatter (or any user-defined profile), and behave according to its guidance before parsing the rest of this prompt.

- **Profile**: `implementer-ivan`
- **Role**: `implementer`
- **Agent/tool**: `claude`

If no profile is specified, run `spec-kitty agent profile list` and select the best match for this work package's `task_type` and `authoritative_surface`.

---

## ⚠️ IMPORTANT: Review Feedback

**Read this first if you are implementing this task!**

- **Has review feedback?**: Check the `review_ref` field in the event log (via `spec-kitty agent status` or the Activity Log below).
- **You must address all feedback** before your work is complete. Feedback items are your implementation TODO list.
- **Report progress**: As you address each feedback item, update the Activity Log explaining what you changed.

---

## Review Feedback

*[If this WP was returned from review, the reviewer feedback reference appears in the Activity Log below or in the status event log.]*

---

## Markdown Formatting

Wrap HTML/XML tags in backticks: `` `<div>` ``, `` `<script>` ``
Use language identifiers in code blocks: ````python`, ````bash`

---

## Objectives & Success Criteria

Establish the preconditions all conversion WPs depend on:

- `docs/development/quality-and-tech-debt-standing-orders.md` committed with valid frontmatter (description 50-180 chars) and registered in the docs page inventory via the canonical freshener script.
- Docs-freshness CI gate passes (frontmatter + inventory row).
- Epic #2196 is the genuine functional epic (scope-tracker framing removed, tracker-only edit).

**Plan decisions recorded here (NOT subtasks):**
- **PD-1**: Directive numbers 043-049 are reserved for this mission. Highest existing built-in is 042. New directive assignments: 043 = §5a arch-gate-construction, 044 = §6 canonical-sources, 045 = §7 PRs-only/read-intent. Numbers 046-049 are reserved; any conversion that does not author a directive returns its reserved number. No WP mints a number ad-hoc.
- **PD-2**: `graph.yaml` is generated by `src/doctrine/drg/migration/extractor.py:generate_graph`. Conversion WPs author inline `requires`/`suggests`/`refines` edges in their artifact YAML; the single `graph.yaml` regeneration is owned by WP12 (the wiring WP). This removes `graph.yaml` as a parallel-write collision surface.

## Context & Constraints

- **Source content**: `kitty-specs/doctrine-catfooding-2196-01KWE16N/research/quality-and-tech-debt-standing-orders.source.md` is the canonical source. Copy its content faithfully into the new doc — do NOT paraphrase or restructure.
- **Freshener script**: `scripts/docs/freshen_adr_inventory.py` / `inventory_lockfile.py` (from PR #2282) is the canonical way to register a new doc. Do NOT hand-edit `docs/development/3-2-page-inventory.yaml` directly.
- **Tail-conflict**: A trivial tail-append conflict with PR #2277 on `3-2-page-inventory.yaml` is expected. When it occurs, regenerate the inventory file via the freshener script; do not hand-merge.
- **Epic re-parenting**: epic #2196 is labeled `scope-tracker`/"not a functional parent" but natively parents all children. The fix is to drop that framing (update the GitHub issue label/description via `gh`) so #2196 is a genuine functional epic. This is a tracker-only edit — no code or docs.
- **Docs-freshness gate**: `pytest tests/architectural/ -k freshness` (or equivalent) catches missing inventory rows and malformed frontmatter. This is a CI-only gate; run it locally before pushing.

## Branch Strategy

- **Strategy**: Coord topology. Planning artifacts reside on `design/doctrine-catfooding-2196`. WP execution lanes branch from the coordination branch `kitty/mission-doctrine-catfooding-2196-01KWE16N` and merge back into it.
- **Planning base branch**: `design/doctrine-catfooding-2196`
- **Merge target branch**: `kitty/mission-doctrine-catfooding-2196-01KWE16N`

> These fields are populated automatically by `spec-kitty agent mission tasks`.
> Do NOT change them manually unless you are certain the branch topology has changed.

## Subtasks & Detailed Guidance

### Subtask T001 – Commit source doc with valid frontmatter

- **Purpose**: Create the canonical human-readable source document that the doctrine conversions convert *from* and that the charter later mirrors. Must pass the docs-freshness frontmatter check (description 50-180 chars).
- **Steps**:
  1. Create `docs/development/quality-and-tech-debt-standing-orders.md`.
  2. Add YAML frontmatter with at minimum a `description` field of 50-180 characters summarising the 8-section standing orders.
  3. Copy the content faithfully from `kitty-specs/doctrine-catfooding-2196-01KWE16N/research/quality-and-tech-debt-standing-orders.source.md`. Preserve structure; do not paraphrase.
  4. Commit with a message referencing the mission slug and FR-001.
- **Files**: `docs/development/quality-and-tech-debt-standing-orders.md` (create new)
- **Parallel?**: No — T002 depends on this file existing.
- **Notes**: The frontmatter description must be between 50 and 180 characters or the docs-freshness gate will reject the commit in CI.

### Subtask T002 – Register in page inventory via canonical freshener

- **Purpose**: Ensure the source doc appears in the docs page inventory so docs-freshness CI gates pass.
- **Steps**:
  1. Run `python scripts/docs/freshen_adr_inventory.py docs/development/quality-and-tech-debt-standing-orders.md` (or the equivalent `inventory_lockfile.py` invocation — check PR #2282 for the exact command).
  2. If a tail-append conflict with PR #2277 arises on `docs/development/3-2-page-inventory.yaml`, regenerate the whole inventory via the freshener script rather than hand-merging.
  3. Stage and commit `docs/development/3-2-page-inventory.yaml` alongside any changes.
- **Files**: `docs/development/3-2-page-inventory.yaml` (update existing)
- **Parallel?**: No — must run after T001.
- **Notes**: Never hand-edit the page inventory YAML directly; it is a generated lockfile.

### Subtask T003 – Verify docs-freshness gates pass

- **Purpose**: Confirm CI-only docs-freshness gates will not fail before the branch is pushed.
- **Steps**:
  1. Run `pytest tests/architectural/ -k freshness -q` (or equivalent) locally.
  2. If any test fails due to the new doc: inspect the error message (usually missing inventory row or malformed frontmatter description), fix, and re-run.
  3. All freshness tests must be green before moving this WP to `for_review`.
- **Files**: No files changed in this subtask — verification only.
- **Parallel?**: No — must run after T001+T002.
- **Notes**: Docs-freshness is a CI-only gate; a passing local run is required before pushing.

### Subtask T004 – Re-parent epic #2196

- **Purpose**: Drop the `scope-tracker` / "not a functional parent" framing on GitHub issue #2196 so it is a genuine functional epic (FR-004/PD-3).
- **Steps**:
  1. Run `unset GITHUB_TOKEN && gh issue edit 2196 --repo Priivacy-ai/spec-kitty --remove-label scope-tracker` (adjust label name if different).
  2. Update the issue body or description to remove any "not a functional parent" language.
  3. Confirm with `gh issue view 2196 --repo Priivacy-ai/spec-kitty` that the label is gone and the description is clean.
- **Files**: None (tracker-only edit).
- **Parallel?**: [P] Can proceed alongside T001-T003.
- **Notes**: This is purely a GitHub tracker change. No code, no docs. Do not open a PR for this subtask.

## Test Strategy

- `pytest tests/architectural/ -k freshness -q` — must be green after T002.
- Verify `docs/development/quality-and-tech-debt-standing-orders.md` frontmatter description is 50-180 chars with a character-count command if uncertain:
  ```bash
  python -c "import yaml; f=open('docs/development/quality-and-tech-debt-standing-orders.md').read(); fm=f.split('---')[1]; d=yaml.safe_load(fm)['description']; print(len(d), repr(d))"
  ```
- Verify the file appears in `docs/development/3-2-page-inventory.yaml` with the correct path key after T002.
- After T004: confirm `gh issue view 2196 --repo Priivacy-ai/spec-kitty` shows no `scope-tracker` label and the description does not contain "not a functional parent" language.

## Risks & Mitigations

- **Docs-freshness CI-only gate**: The freshness test suite is CI-only (`fast-tests-*` does not include it); run the architectural tests locally before pushing to avoid a surprise CI-only failure. The command `pytest tests/architectural/ -k freshness -q` simulates what CI does.
- **Tail-conflict with PR #2277**: Expected on `docs/development/3-2-page-inventory.yaml` — a trivial tail-append conflict. Re-generate the inventory file via the canonical freshener script, then commit; do not hand-merge.
- **GitHub auth**: If `gh` fails with "Missing required token scopes" on the org repo, run `unset GITHUB_TOKEN && gh auth status` to verify the keyring token (gho_* with full `repo` scope) is active. The `GITHUB_TOKEN` env var may shadow the keyring token with a limited-scope token.
- **Research source path**: the source lives at `kitty-specs/doctrine-catfooding-2196-01KWE16N/research/quality-and-tech-debt-standing-orders.source.md` — read it before writing T001. Do not summarise; copy the content faithfully and wrap in the standard doc frontmatter.
- **Frontmatter description length**: descriptions shorter than 50 chars or longer than 180 chars will fail the freshness gate at CI. Count the characters before committing.
- **Content faithfulness**: the source doc must be a faithful copy of the research source — not a paraphrase. If the research source is authored in Markdown with 8 section headings, the committed doc preserves that structure exactly.

## Dependency Notes

- WP01 blocks ALL conversion WPs (WP02-WP11). Do not claim any conversion WP until WP01 is `approved`.
- PD-1 and PD-2 are plan decisions encoded in WP01's implementation notes — they constrain every other WP but do not require implementation steps here.
- `tasks.md` declares WP01 as the root blocker; the subtask index will show WP01's T001-T004 as prerequisites for the entire LA/LB execution wave. The dependency graph is: WP01 → [WP02 → WP03, WP04, WP05, WP06, WP07, WP08, WP09, WP10, WP11] → WP12 → WP13.

## Review Guidance

- Source doc content faithfully mirrors `research/quality-and-tech-debt-standing-orders.source.md` — spot-check 3 random paragraphs for accuracy.
- Frontmatter `description` is 50-180 chars (check with the Python command from Test Strategy).
- `docs/development/3-2-page-inventory.yaml` contains a row for the new doc at its canonical path.
- `pytest tests/architectural/ -k freshness -q` green.
- Epic #2196 has no `scope-tracker` label; description does not contain "not a functional parent."
- Commit message references the mission slug (`doctrine-catfooding-2196`) and `FR-001`.
- PD-1 (043-049 reserved) and PD-2 (graph.yaml single-regen at WP12) are cited in the Activity Log as recorded decisions — not implemented, just noted.

## Parallel Opportunities

Within this WP: T001 → T002 → T003 are sequential (each depends on the prior). T004 (epic re-parent) is independent — it can run in parallel with T001-T003. WP01 as a whole blocks all conversion WPs; get it to `approved` as quickly as possible to unblock the parallel LA/LB conversions.

## Updating Status

Status is managed via `status.events.jsonl`. Use `spec-kitty agent tasks move-task WP01 --to claimed` after starting, `--to for_review` when subtasks are complete.

Record the following in the Activity Log before moving to `for_review`:
1. Confirmation that PD-1 (directive reservation 043-049) is noted as a plan decision — no directive numbers minted in this WP, no YAML authored in `src/doctrine/directives/`.
2. Confirmation that PD-2 (single graph regen at WP12) is noted — `src/doctrine/graph.yaml` is NOT touched in this WP.
3. Docs-freshness test result (green/red) and the exact command run (include the output snippet).
4. Epic #2196 re-parent outcome: label removed, `gh issue view` output confirming the change.
5. Commit SHA(s) for the source doc commit and the page-inventory update commit (may be the same or separate commits).
6. Outcome of `spec-kitty agent tasks status --feature doctrine-catfooding-2196-01KWE16N` confirming WP01 reached `for_review`.

## Activity Log

> **CRITICAL**: Activity log entries MUST be in chronological order (oldest first, newest last).
> Do not pre-fill future entries. Append when you actually complete a step.
> Format: `YYYY-MM-DDTHH:MM:SSZ – <actor> – <description of action taken>`

- 2026-07-01T06:14:46Z – system – Prompt generated via /spec-kitty.tasks (13 WPs, 63 subtasks)
- 2026-07-01T09:49:51Z – claude:sonnet:implementer-ivan:implementer – shell_pid=1670323 – Assigned agent via action command
- 2026-07-01T09:50:08Z – claude:sonnet:implementer-ivan:implementer – shell_pid=1673629 – Assigned agent via action command
- 2026-07-01T09:58:03Z – claude:sonnet:implementer-ivan:implementer – shell_pid=1673629 – Ready: source doc committed (73905750c in lane-a) + inventoried (docs-freshness exit=0), #2196 re-parented (scope-tracker label removed, body cleaned). PD-1: directives 043-049 reserved, none authored here. PD-2: graph.yaml not touched, single regen at WP12. Terminology gate: 3 passed.
- 2026-07-01T09:58:35Z – claude:opus:reviewer-renata:reviewer – shell_pid=1699534 – Started review via action command
- 2026-07-01T10:03:21Z – user – shell_pid=1699534 – Review passed: source doc byte-faithful copy of research source (body diff identical) + valid frontmatter (description 176 chars, within 50-180); inventory registered via canonical lockfile (freshness --ci --link-check none exit=0, errors=0, only pre-existing HELP-DRIFT warnings); terminology guard 3 passed; #2196 re-parented (scope-tracker label removed, framing stripped); diff scope = 2 owned files only; PD-1/PD-2 correctly NOT implemented (no src/doctrine or graph.yaml). Issue-matrix filled to pass per-WP gate; terminal verdicts required before mission done.
