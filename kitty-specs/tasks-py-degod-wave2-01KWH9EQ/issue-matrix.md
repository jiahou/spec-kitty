# Issue matrix — tasks-py-degod-wave2-01KWH9EQ

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #2305 | Follow-up: finish tasks.py degod — Render seam unification + whole-file shim relocation (Wave 2, #2173) | fixed | DELIVERED: Stream A (render seam unified, _StatusRender deleted, AST gate + theater tests) + Stream B (all 5 families + shared helpers + adapters relocated; tasks.py 4569→1206 LOC; ceiling gate min-capped at 1400; shim deleted). 10/10 WPs approved. |
| #2173 | Epic: Infrastructure-to-logic separation | deferred-with-followup | Parent epic — this mission executes its Wave 2 tasks.py slice under C-004 seam discipline (one production adapter per port, builder/shell injection). Follow-up: #2173 (epic remains open for Phase 1/2 port work). |
| #2034 | CI gates select tests by marker; no gate selects `unit` or `contract` | deferred-with-followup | Domain slice DELIVERED (WP10): FR-009 census artifact `marker-census.md` (43 files / 754 tests / zero unselected) + standing baseline-growth assertion `tests/architectural/test_tasks_domain_gate_visibility.py`; FR-010 final refresh drafted (`2034-refresh-draft.md` — orchestrator posts at mission review). Upstream verdict stays deferred-with-followup: repo-wide fix is #2283/#2296/#2297. |
| #2300 | Unify the coord+protected skip-vs-refuse behavior | deferred-with-followup | Deliberately NOT touched (C-001): divergence preserved verbatim; reconciliation is #2300's own behavior-change mission. |
| #2116 | Decompose agent tasks.py god-command | fixed | Wave 1 closed the body-thinning portion; Wave 2 final state (WP10 verification): `tasks.py` = 1206 LOC registration shim, all five command families relocated to sibling modules (`tasks_move_task.py`, `tasks_map_requirements.py`, `tasks_mark_status.py`, `tasks_finalize.py`, `tasks_status_cmd.py`), LOC gate (FR-011) + AST 0-inline-dumps gate (FR-007) standing in `tests/architectural/`. |
| #2306 | test_untrusted_path_containment RED on main (inventory.md 1325→1326 off-by-one from Wave 1) | fixed | WP05: inventory corrected and the inventory row moved with the move_task family relocation; `test_untrusted_path_containment` gate green on the mission tree (FR-001/FR-002 seam checklist evidence). |
| #2031 | Stale-assertion analyzer lacks cross-file move detection | deferred-with-followup | Reference only: Wave 2's ~40-symbol relocation will trigger a large false-positive storm from the post-merge analyzer at every WP merge (witnessed 280+ on a single-module extraction). Review artifacts must expect noise and cross-check against the FR-002 seam checklists; the analyzer fix is #2031's own work. |
| #2283 | Test-delivery topology: CI-only shards invisible pre-PR | deferred-with-followup | Structural parent of #2034. FR-009 closes its cause (a) marker-gate divergence for the tasks domain only; causes (b) venv skew and (c) producer-conformance sweep remain #2283's scope. FR-010's census comment references it. |
| #2295 | Triage the 17 CI-quarantined tests (Wave-0 orphan binding) | deferred-with-followup | Reference only: defines the current `_gate_coverage_baseline.json` floor (4 orphan paths, none in the tasks domain). FR-009's baseline-growth prohibition is measured against this floor. Follow-up: #2295 (triage stays upstream). |
| #2303 | Wave 1 degod PR (tasks-py-degod-01KWF08S) | verified-already-fixed | Predecessor PR, MERGED to upstream main as `381db8d5f` (this mission's base, C-002); referenced as lineage only. |
| #2289 | Unshim: delete the 8 category_4 backcompat re-export shims | deferred-with-followup | Boundary only: `tasks_ports.py` is NOT on #2289's 8-shim list; FR-008 owns that single shim's disposition (ownership fence comment posted 2026-07-02). Follow-up: #2289 (cluster stays upstream). |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
