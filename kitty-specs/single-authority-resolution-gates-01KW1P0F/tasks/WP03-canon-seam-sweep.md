---
work_package_id: WP03
title: Canonicalizer seam-module sweep (_read_path_resolver, mission_type, retrospective)
dependencies:
- WP01
requirement_refs:
- FR-005
tracker_refs: []
planning_base_branch: design/infra-logic-separation-2173
merge_target_branch: design/infra-logic-separation-2173
branch_strategy: Planning artifacts for this mission were generated on design/infra-logic-separation-2173. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into design/infra-logic-separation-2173 unless the human explicitly redirects the landing branch.
subtasks:
- T015
- T016
- T017
- T018
agent: "claude:sonnet:reviewer-renata:reviewer"
shell_pid: "3065322"
history:
- timestamp: '2026-06-26T11:02:04Z'
  actor: planner
  action: created
  note: Generated by /spec-kitty.tasks
agent_profile: python-pedro
authoritative_surface: src/specify_cli/missions/
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/missions/_read_path_resolver.py
- src/specify_cli/cli/commands/mission_type.py
- src/specify_cli/retrospective/writer.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile

Before reading anything else, load your assigned profile so it governs how you implement:
`/ad-hoc-profile-load python-pedro`
Do not skip this.

## Objective

Sanction or route every `primary_feature_dir_for_mission` call site in the three
seam-module files — `_read_path_resolver.py` (7 call sites, mostly seam-internal),
`mission_type.py` (2 sites), and `retrospective/writer.py` (1 site) — so the
canonicalizer gate (built in WP01) is GREEN for these files, and remove any entries
these actions route away from the pre-sweep baseline allowlist.

**Routing is the default.** Allowlisting is the justified exception: each allowlist
entry must name an already-canonical provenance (e.g. the arg is already a
`_canonicalize_primary_read_handle` return or a `feature_dir.name` in the same
function). The seam-internal sites in `_read_path_resolver.py` are predominantly
already-canonical, but every site must be inspected and an allowlist rationale recorded.

**Merge-blocker C-001:** `_read_path_resolver.py:454` is the recursion probe inside
`_canonicalize_primary_read_handle`. It MUST remain a bare-handle call and must be
pinned in the allowlist with the FR-011 rationale (folding canonicalization into the
primitive recurses forever). Do not "fix" it — do not add `_canonicalize_primary_read_handle`
wrapping to line 454.

**Merge-blocker C-002:** Every site that does route through `_canonicalize_primary_read_handle`
must propagate `MissionSelectorAmbiguous` unchanged. The canonicalizer already raises
on ambiguity — do not swallow or catch it at the call site.

## Context

- Spec: `kitty-specs/single-authority-resolution-gates-01KW1P0F/spec.md` (FR-005, C-001, C-002)
- Plan: `kitty-specs/single-authority-resolution-gates-01KW1P0F/plan.md` (IC-02b)
- Contracts: `kitty-specs/single-authority-resolution-gates-01KW1P0F/contracts/resolution-gates.md` (Contract 1)
- Foundation: WP01 builds `tests/architectural/test_resolution_authority_gates.py` +
  `tests/architectural/resolution_gate_allowlist.yaml` (the shrink-only baseline this
  WP edits). Do not run before WP01 is approved.

**Shared routing recipe:** wherever `primary_feature_dir_for_mission(repo_root, <handle>)` is
called and `<handle>` is a bare or un-canonicalized mission slug, wrap it as:
`primary_feature_dir_for_mission(repo_root, _canonicalize_primary_read_handle(repo_root, handle))`
or use the established seam helper. Import `_canonicalize_primary_read_handle` from
`src/specify_cli/missions/_read_path_resolver.py:1244` (the canonical fold). Record an
allowlist entry only when the handle is provably already canonical — `feature_dir.name`
from a prior resolution, the return of `_canonicalize_primary_read_handle`, or `canonical_handle`
named so in the same function.

## Subtask Guidance

### T015 — Sanction `_read_path_resolver.py` seam-internal sites; pin `:454` (C-001)

**Owned file:** `src/specify_cli/missions/_read_path_resolver.py`

Live call sites discovered by `git grep -n "primary_feature_dir_for_mission("`:

| Line | Snippet | Status |
|------|---------|--------|
| `:454` | `literal_primary = primary_feature_dir_for_mission(repo_root, handle)` | PIN — recursion probe inside `_canonicalize_bare_modern_handle` (def at `:418`); bare-handle is intentional by FR-011. Note: the enclosing function is `_canonicalize_bare_modern_handle`, NOT `_canonicalize_primary_read_handle` (def `:1244`) — use `_canonicalize_bare_modern_handle` as the allowlist qualname for this entry. |
| `:820` | `primary_dir = primary_feature_dir_for_mission(repo_root, handle)` | Inspect caller: `handle` originates from an identity-resolver cascade; verify provenance — likely already canonical within `_resolve_mission_read_path` |
| `:950` | `primary_feature_dir_for_mission(repo_root, handle)` inside `classify_from_meta` call | Inspect: same seam cascade; verify `handle` is canonical at that depth |
| `:972` | `primary_candidate = primary_feature_dir_for_mission(repo_root, handle)` | Inspect: appears in a coord-window guard; verify whether `handle` is already folded |
| `:1023` | `primary_candidate=primary_feature_dir_for_mission(repo_root, handle)` (kwarg) | Inspect: note the keyword-arg call form — the gate discriminator handles kwargs, record rationale |
| `:1208` | `primary_dir = primary_feature_dir_for_mission(repo_root, canonical_handle)` | Already canonical — `canonical_handle = _canonicalize_bare_modern_handle(repo_root, mission_slug)` is assigned on `:1204`; sanction |
| `:1368` | `return primary_feature_dir_for_mission(repo_root, canonical)` | Already canonical — `canonical = _canonicalize_primary_read_handle(repo_root, mission_slug)` on `:1367`; sanction |

**Action for each site:**
1. `:454` — Add an allowlist entry with key `(_canonicalize_bare_modern_handle, 454)` (the enclosing function is `_canonicalize_bare_modern_handle`, def at `:418` — NOT `_canonicalize_primary_read_handle`, def at `:1244`) and rationale `"C-001/FR-011 recursion probe: bare-handle probe is the definition of the canonical fold; wrapping it recurses forever"`. Do NOT touch the code. Verify the qualname by checking which `def` statement directly encloses `:454` before writing the entry.
2. `:820`, `:950`, `:972`, `:1023` — Trace the intra-function `handle` provenance. If `handle`
   arrives already from `_canonicalize_primary_read_handle` (or is the result of a prior
   cascade step that guarantees canonical form), record it as `SANCTION` with the rationale
   naming the assignment line. If bare, wrap with `_canonicalize_primary_read_handle`.
3. `:1208`, `:1368` — Sanction: `canonical_handle`/`canonical` are provably from the fold in
   the same function; add allowlist entries recording the canonical-provenance line.

**Validation:** After updating the allowlist, run:
```bash
pytest tests/architectural/test_resolution_authority_gates.py -q -k "canonicalizer"
```
Gate must be GREEN. `:454` must appear in the allowlist; removing it must turn the gate RED.

### T016 — Route/sanction `mission_type.py` (`:592` route, `:1048` already-canonical)

**Owned file:** `src/specify_cli/cli/commands/mission_type.py`

Live call sites:

| Line | Snippet | Action |
|------|---------|--------|
| `:592` | `feature_dir = primary_feature_dir_for_mission(repo_root, mission_slug)` | `mission_slug` is a bare operator-supplied handle — ROUTE through `_canonicalize_primary_read_handle` |
| `:1048` | `feature_dir = primary_feature_dir_for_mission(repo_root, canonical_handle)` | `canonical_handle = _canonicalize_primary_read_handle(repo_root, handle)` assigned on `:1047`; SANCTION |

**Routing `:592`:**
```python
# Before:
feature_dir = primary_feature_dir_for_mission(repo_root, mission_slug)

# After (import _canonicalize_primary_read_handle from missions._read_path_resolver):
feature_dir = primary_feature_dir_for_mission(
    repo_root,
    _canonicalize_primary_read_handle(repo_root, mission_slug),
)
```
Verify the import is present: `from specify_cli.missions._read_path_resolver import _canonicalize_primary_read_handle`.
`_canonicalize_primary_read_handle` raises `MissionSelectorAmbiguous` on ambiguity — do not catch it here (C-002).

**Sanctioning `:1048`:** Add allowlist entry with key `(<enclosing_qualname>, 1048)` and
rationale `"canonical_handle = _canonicalize_primary_read_handle(...) assigned at :1047 in same function"`.

**Validation:**
```bash
pytest tests/architectural/test_resolution_authority_gates.py -q -k "canonicalizer"
ruff check src/specify_cli/cli/commands/mission_type.py
mypy src/specify_cli/cli/commands/mission_type.py --ignore-missing-imports
```

### T017 — Route/sanction `retrospective/writer.py`

**Owned file:** `src/specify_cli/retrospective/writer.py`

Live call site:

| Line | Snippet | Action |
|------|---------|--------|
| `:85` | `feature_dir: Path = primary_feature_dir_for_mission(repo_root, canonical)` | `canonical = _canonicalize_primary_read_handle(repo_root, mission_slug)` assigned on `:84`; SANCTION |

Reading the source confirms `canonical` on `:84` is assigned directly from
`_canonicalize_primary_read_handle(repo_root, mission_slug)` — this is already the
pattern the gate rewards. Add an allowlist entry:
- Key: `(<enclosing function qualname>, 85)`
- Rationale: `"canonical = _canonicalize_primary_read_handle(repo_root, mission_slug) assigned on :84 in same function"`

No code change needed. If the gate still marks this site as a violation, the discriminator's
intra-function def-use detection has a bug — escalate to WP01 rather than adding a broad
workaround.

**Validation:**
```bash
pytest tests/architectural/test_resolution_authority_gates.py -q -k "canonicalizer"
```
The site at `:85` must not appear in the gate's failure output.

### T018 — Record routed seam sites for WP08 allowlist shrink

After completing T015–T017, identify which pre-sweep entries in `tests/architectural/resolution_gate_allowlist.yaml` correspond to sites this WP has now routed (e.g. `:592` in `mission_type.py`) and which are now sanctioned-with-rationale (`:454`, `:1048`, `:1208`, `:1368`, `:85`, and any seam-internal sites proven already-canonical in T015).

> **Allowlist ownership note:** WP08 performs the SINGLE final shrink of `tests/architectural/resolution_gate_allowlist.yaml` — removing every entry whose site is now def-use-canonical (routed). WP03 does NOT edit the shared allowlist YAML to remove pre-sweep entries. This avoids cross-lane edit contention on the shared file. Your DoD is that WP03's routed and sanctioned sites pass the gate (gate is GREEN for these files), not that the YAML is smaller after this WP.

The allowlist is shrink-only (NFR-003): sanctioned-with-rationale entries (newly added in T015–T017) are permitted as out-of-map adds; pre-sweep entries for routed sites wait for WP08 to remove.

**Steps:**
1. Run the gate and confirm zero violations for the WP03-owned files.
2. Record in the commit message body which pre-sweep entries correspond to sites now routed — this is the manifest WP08 consumes.
3. Do NOT remove entries from `tests/architectural/resolution_gate_allowlist.yaml` in this WP.

**Validation:**
```bash
pytest tests/architectural/test_resolution_authority_gates.py -q
```
Both discriminators must be GREEN for the WP03-owned files. The staleness twin-guard may still show pre-sweep entries for other-WP sites (those are their responsibility); WP03's own routed sites must produce no violations.

## Branch Strategy

Work directly on `design/infra-logic-separation-2173` (flattened mission, `branch_strategy: direct`).
Do not create a worktree. Stage and commit only files in `owned_files` plus
`tests/architectural/resolution_gate_allowlist.yaml` (allowlist shrink).
No changes to `src/specify_cli/cli/commands/agent/tasks.py` or `implement.py` — those are
WP02-owned.

## Definition of Done

- [ ] `_read_path_resolver.py:454` is allowlisted with C-001/FR-011 rationale; code unchanged.
- [ ] All other seam-internal `_read_path_resolver.py` sites are either sanctioned (allowlisted
      with already-canonical rationale) or routed through `_canonicalize_primary_read_handle`.
- [ ] `mission_type.py:592` is routed through `_canonicalize_primary_read_handle`.
- [ ] `mission_type.py:1048` is sanctioned (allowlist entry names `:1047` canonical assignment).
- [ ] `retrospective/writer.py:85` is sanctioned (allowlist entry names `:84` canonical assignment).
- [ ] WP03's routed sites pass the canonicalizer gate as def-use-canonical; routed-site manifest recorded in commit message for WP08's final shrink. `resolution_gate_allowlist.yaml` is NOT modified to remove pre-sweep entries in this WP (WP08 owns the shrink).
- [ ] `pytest tests/architectural/test_resolution_authority_gates.py -q` is GREEN.
- [ ] `ruff check` and `mypy` pass with zero issues on all modified files.
- [ ] No changes to `git/commit_helpers.py` (C-006 merge-blocker — guard is read-only).

## Risks

- **Double-fold:** If a site is routed AND left in the allowlist, the twin-guard fails.
  Remove allowlist entries for all routed sites.
- **`:454` "fixed" accidentally:** Wrapping `:454` recurses forever. The C-001 check in the
  gate regression-pins it — if you accidentally touch it the gate should catch the recursion
  but check anyway.
- **Missing keyword-arg detection:** The gate discriminator handles keyword-arg calls
  (`primary_feature_dir_for_mission(repo_root, handle=...)`) — verify `:1023` is checked
  correctly, as it uses a keyword-arg form.
- **Ambiguity swallowing:** If `_canonicalize_primary_read_handle` is wrapped in a
  `try/except MissionSelectorAmbiguous` at a new routing site, that is a C-002 violation.
  Do not catch it; let it propagate.

## Reviewer Guidance

Reviewers should confirm:
1. `:454` is unchanged in `_read_path_resolver.py` and appears in the allowlist with the
   FR-011 recursion rationale.
2. Every routing change wraps `_canonicalize_primary_read_handle` — not
   `_canonicalize_bare_modern_handle` or any other partial fold.
3. No site has both a routing change AND an allowlist entry.
4. The staleness twin-guard is GREEN (no leftover allowlist entries for now-routed sites).
5. `mission_type.py:592` has the `_canonicalize_primary_read_handle` import and call;
   `MissionSelectorAmbiguous` is not caught.
6. `ruff` and `mypy` are clean on all touched files.

## Activity Log

- 2026-06-26T12:36:45Z – claude:sonnet:python-pedro:implementer – shell_pid=2931106 – Assigned agent via action command
- 2026-06-26T13:01:55Z – claude:sonnet:python-pedro:implementer – shell_pid=2931106 – Ready: mission_type.py:592 routed through _canonicalize_primary_read_handle; _read_path_resolver.py seam-internal sites :820/:950/:972/:1023/:1208 sanctioned; gate 30/30 GREEN; ruff+mypy clean on WP03-owned files; commit f97e143b6
- 2026-06-26T13:14:43Z – claude:sonnet:reviewer-renata:reviewer – shell_pid=3065322 – Started review via action command
- 2026-06-26T13:19:34Z – user – shell_pid=3065322 – Review passed (renata): contamination-clean (mission_type.py + allowlist only), close_cmd routed, :454 pinned, gate 30/30, 9 close_cmd integration tests pass, C-002 propagates, ruff/mypy clean.
