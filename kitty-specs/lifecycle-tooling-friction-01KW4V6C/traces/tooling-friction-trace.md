# Tooling-Friction Trace — Mission-Lifecycle Tooling Friction

(Meta: this mission FIXES lifecycle tooling friction; expect to hit the very frictions it targets.)

## Planning
- `spec-kitty specify` defaulted to `topology: coord` + minted a coord branch — the exact
  #2218 friction. Flattened to `topology: lanes` by hand (drop coordination_branch, set
  topology=lanes, delete the coord branch). Live motivation evidence for FR-004.

## Implementation
- _(append during implement)_
