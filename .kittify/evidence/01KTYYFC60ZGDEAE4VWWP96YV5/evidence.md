PASS: #1684 fix faithful to debby (fresh+reuse+coord+legacy merge dep tips in parallel_group order; warn+skip on missing branch faithful; fail-closed conflict aborts before structured error). Randy reduction behavior-preserving (if/else fold + _create_branch_from byte-identical msgs). Seam clean via lane_branch_name; topology ratchet green. 4a --base composition test WRITTEN+committed. 4b cross-module _branch_exists non-blocking. Gates: regression 6, lanes 237, architectural 353, ruff clean, zero NEW mypy.